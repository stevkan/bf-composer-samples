{
  "$schema": "https://schemas.botframework.com/schemas/component/v1.0/component.schema",
  "$role": "implements(Microsoft.IDialog)",
  "title": "Dynamic Dialog Loading",
  "description": "If dialog passed isn't find, it loads it just-in-time",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "dialog": {
      "$ref": "schema:#/definitions/stringExpression",
      "title": "Dialog",
      "description": "Dialog name to be loaded dynamically"
    }
  }
}
